# ── Android ──
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/keystore.properties

# ── 機密 ──
release/*.keystore
release/*.jks
server/.env

# ── CI（需 workflow scope 授權後再由 enable-ci.sh 推送）──
.github/workflows/

# ── 一般 ──
.wrangler/
.DS_Store
*.iml
.idea/
node_modules/
*.log